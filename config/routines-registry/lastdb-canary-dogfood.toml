# Canary v2 primary action: resolve Forge fold main and run one bounded
# safe-upgrade action. The hourly reconciler owns the quiet window.
id = "lastdb-canary-dogfood"
harness = "claude"
model = "sonnet"
effort = "medium"
group = "dogfood"
rrule = "FREQ=DAILY;BYHOUR=3;BYMINUTE=17;BYSECOND=0"
cwd = "/Users/REPLACE/code/edgevector"
status = "active"
timeout_min = 120
prompt_path = "/Users/REPLACE/.last-stack/routines/lastdb-canary-dogfood.md"
gate_command = "/Users/REPLACE/.last-stack/bin/last-stack-canary-v2-dogfood-gate"
heartbeat_slug = "routine-heartbeats"
