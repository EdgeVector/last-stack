# Refresh LastDB Mini telemetry dashboard Artifact (read-only on primary brain).
# Prompt lives under last-stack/routines; extracts regen script from fold mirror
# (never $HOME/code/edgevector/fold portal path).
id = "lastdbd-mini-telemetry-dashboard-refresh"
harness = "grok"
model = "grok-4.5"
fallback      = "grok:grok-4.5"
rrule = "FREQ=DAILY;BYHOUR=15;BYMINUTE=0;BYSECOND=0"
cwd = "/Users/REPLACE/code/edgevector"
status = "active"
timeout_min = 30
heartbeat_slug = "routine-heartbeats"
prompt_path = "/Users/REPLACE/.last-stack/routines/lastdbd-mini-telemetry-dashboard-refresh.md"
