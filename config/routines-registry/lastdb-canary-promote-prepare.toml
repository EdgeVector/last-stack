# Auto release loop: after soak_green, promote-execute to public brew stable (local-canonical).
# Registry id kept as promote-prepare for continuity; prompt runs promote-execute.
id = "lastdb-canary-promote-prepare"
harness = "claude"
model = "sonnet"
effort = "medium"
group = "ops"
rrule = "FREQ=HOURLY;INTERVAL=1;BYMINUTE=35;BYSECOND=0"
cwd = "/Users/REPLACE/code/edgevector"
status = "active"
timeout_min = 90
prompt_path = "/Users/REPLACE/.last-stack/routines/lastdb-canary-promote-prepare.md"
heartbeat_slug = "routine-heartbeats"
