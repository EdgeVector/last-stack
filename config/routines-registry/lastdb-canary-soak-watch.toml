# LastDB nightly canary soak watcher.
# Paused until north-star-lastdb-nightly-canary-pipeline terminal proof passes.
id = "lastdb-canary-soak-watch"
harness = "codex"
model = "gpt-5.5"
effort = "low"
group = "lastdb-canary"
rrule = "FREQ=HOURLY;INTERVAL=1;BYMINUTE=34;BYSECOND=0"
cwd = "/Users/REPLACE/code/edgevector"
status = "paused"
timeout_min = 10
prompt_path = "/Users/REPLACE/.last-stack/routines/lastdb-canary-soak-watch.md"
heartbeat_slug = "routine-heartbeats"
