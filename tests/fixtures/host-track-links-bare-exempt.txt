# Exemptions for tests/last-stack-host-track-links-bare-helpers.py.
#
# One line per helper: `<bin-name> <reason>`. List a helper here only when a
# skill or routine runs it by bare name on purpose and it must NOT get a
# ~/.local/bin link (for example, it only runs inside a routine whose PATH
# already carries $last_stack/bin). The test fails on a stale line.
#
# Seeded 2026-09-23 empty: every bare-called helper found then got a link.
